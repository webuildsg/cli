{
  "name": "webuild-cli",
  "version": "1.0.0",
  "description": "command line app to query events and repos from webuild",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^2.8.1",
    "file-exists": "^2.0.0",
    "request": "^2.56.0"
  }
}
